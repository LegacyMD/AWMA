%T = 

iteration = 0;

while 1
    fileName = strcat('points_', num2str(iteration), '.csv');
    if ~isfile(fileName)
       break
    end
    
    T = readtable('points_0.csv');
    
    iteration = iteration + 1;
end


disp(iteration);